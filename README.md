# Apex Gaming Cafe

Admin portal for Apex Gaming Cafe — device sessions, snacks, cash register, and billing.

## Run locally

```bash
npm install
npm start
```

Open http://localhost:3000

## Deploy on Render

See `render.yaml`. Set `DATABASE_PATH=/var/data/cafe.db` with a persistent disk on Starter plan.
